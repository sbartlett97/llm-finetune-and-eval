# CUDA 12.1 — Docker image and CUDA 12.x hardware
# cu121 binaries also run on CUDA 12.4+ and CUDA 13.x (backward compatible)
--index-url https://download.pytorch.org/whl/cu121
torch~=2.6.0

-r requirements-base.txt

# flash_attn omitted — the codebase falls back to sdpa automatically
# To add flash_attn 2.x: pip install flash-attn --no-build-isolation
