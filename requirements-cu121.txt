# CUDA 12.1 — Docker image and CUDA 12.x hardware
# cu121 binaries also run on CUDA 12.4+ and CUDA 13.x (backward compatible)
--index-url https://download.pytorch.org/whl/cu121
torch~=2.6.0

-r requirements-base.txt

# Unsloth — 2x faster training and inference, 70% less VRAM
# Unsloth selects its own attention kernels; no separate flash_attn needed
unsloth
